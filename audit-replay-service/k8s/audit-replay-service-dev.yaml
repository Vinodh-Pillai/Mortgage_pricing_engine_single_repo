apiVersion: v1
kind: Namespace
metadata:
  name: wcpe-dev
  labels:
    app.kubernetes.io/part-of: world-class-pricing-engine
    environment: dev
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: audit-replay-service-config
  namespace: wcpe-dev
data:
  SPRING_DATASOURCE_URL: jdbc:postgresql://catalog-postgres.wcpe-dev.svc.cluster.local:5432/catalog
  SPRING_DATASOURCE_USERNAME: catalog_app
  SPRING_DATASOURCE_PASSWORD: catalog_app
  SPRING_FLYWAY_TABLE: audit_replay_flyway_schema_history
  SPRING_FLYWAY_VALIDATE_ON_MIGRATE: "false"
  SPRING_FLYWAY_BASELINE_ON_MIGRATE: "true"
  SPRING_PROFILES_ACTIVE: dev
  SERVER_PORT: "8086"
  MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED: "true"
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,prometheus
  WCPE_SYNTHETIC_ONLY: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-replay-service
  namespace: wcpe-dev
  labels:
    app: audit-replay-service
    app.kubernetes.io/part-of: world-class-pricing-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-replay-service
  template:
    metadata:
      labels:
        app: audit-replay-service
        app.kubernetes.io/part-of: world-class-pricing-engine
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: audit-replay-service
          image: 192.168.4.102:32061/mortgage-pricing/audit-replay-service:0.1.0-k8s
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8086
          envFrom:
            - configMapRef:
                name: audit-replay-service-config
          env:
            - name: JAVA_TOOL_OPTIONS
              value: >-
                -Dspring.flyway.table=audit_replay_flyway_schema_history
                -Dspring.flyway.validate-on-migrate=false
                -Dspring.flyway.baseline-on-migrate=true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 750m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: audit-replay-service
  namespace: wcpe-dev
spec:
  type: ClusterIP
  selector:
    app: audit-replay-service
  ports:
    - name: http
      port: 8086
      targetPort: http
