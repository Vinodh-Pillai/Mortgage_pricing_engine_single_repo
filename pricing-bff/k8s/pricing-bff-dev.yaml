apiVersion: v1
kind: Namespace
metadata:
  name: wcpe-dev
  labels:
    app.kubernetes.io/part-of: world-class-pricing-engine
    environment: dev
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: pricing-bff-config
  namespace: wcpe-dev
data:
  SPRING_PROFILES_ACTIVE: dev
  SERVER_PORT: "8080"
  MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED: "true"
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,prometheus
  WCPE_SYNTHETIC_ONLY: "false"
  LOANWEFT_INTEGRATIONS_QUOTE_SERVICE_BASE_URL: http://quote-service.wcpe-dev.svc.cluster.local:8080
  LOANWEFT_INTEGRATIONS_LOCK_SERVICE_BASE_URL: http://lock-service.wcpe-dev.svc.cluster.local:8080
  LOANWEFT_INTEGRATIONS_MARGIN_SERVICE_BASE_URL: http://margin-service.wcpe-dev.svc.cluster.local:8080
  TENANT_CONTEXT_URL: http://tenant-context-service.wcpe-dev.svc.cluster.local:8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pricing-bff
  namespace: wcpe-dev
  labels:
    app: pricing-bff
    app.kubernetes.io/part-of: world-class-pricing-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pricing-bff
  template:
    metadata:
      labels:
        app: pricing-bff
        app.kubernetes.io/part-of: world-class-pricing-engine
    spec:
      automountServiceAccountToken: false
      tolerations:
      - key: "node.kubernetes.io/disk-pressure"
        operator: "Exists"
        effect: "NoSchedule"
      - key: "node.kubernetes.io/memory-pressure"
        operator: "Exists"
        effect: "NoSchedule"
      - key: "node.kubernetes.io/pid-pressure"
        operator: "Exists"
        effect: "NoSchedule"
      - key: "node.kubernetes.io/unschedulable"
        operator: "Exists"
        effect: "NoSchedule"
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: pricing-bff
          image: 192.168.4.102:32061/mortgage-pricing/pricing-bff:0.1.0-quickquote-loanpass-release-20260702T053053Z-amd64
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: pricing-bff-config
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 750m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: pricing-bff
  namespace: wcpe-dev
spec:
  type: ClusterIP
  selector:
    app: pricing-bff
  ports:
    - name: http
      port: 8080
      targetPort: http
