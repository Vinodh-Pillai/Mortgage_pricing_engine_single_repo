apiVersion: v1
kind: Namespace
metadata:
  name: wcpe-dev
  labels:
    app.kubernetes.io/part-of: world-class-pricing-engine
    environment: dev
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: adjustment-service-config
  namespace: wcpe-dev
data:
  SPRING_PROFILES_ACTIVE: dev
  SERVER_PORT: "8080"
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,prometheus
  WCPE_SYNTHETIC_ONLY: "true"
  SPRING_DATASOURCE_URL: jdbc:postgresql://catalog-postgres.wcpe-dev.svc.cluster.local:5432/catalog
  SPRING_DATASOURCE_USERNAME: catalog_app
  SPRING_FLYWAY_TABLE: adjustment_flyway_schema_history
  SPRING_FLYWAY_VALIDATE_ON_MIGRATE: "false"
  SPRING_FLYWAY_BASELINE_ON_MIGRATE: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adjustment-service
  namespace: wcpe-dev
  labels:
    app: adjustment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adjustment-service
  template:
    metadata:
      labels:
        app: adjustment-service
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: adjustment-service
          image: 192.168.4.102:32061/mortgage-pricing/adjustment-service:0.1.0-k8s
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: adjustment-service-config
          env:
            - name: JAVA_TOOL_OPTIONS
              value: >-
                -Dspring.flyway.table=adjustment_flyway_schema_history
                -Dspring.flyway.validate-on-migrate=false
                -Dspring.flyway.baseline-on-migrate=true
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: catalog-db-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 750m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: adjustment-service
  namespace: wcpe-dev
spec:
  type: ClusterIP
  selector:
    app: adjustment-service
  ports:
    - name: http
      port: 8080
      targetPort: http
