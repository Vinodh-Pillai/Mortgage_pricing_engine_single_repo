{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://codefactory.local/contracts/compliance/compliance-shell.schema.json",
  "title": "ComplianceShellEvaluation",
  "type": "object",
  "additionalProperties": false,
  "required": ["requestId", "subjectRef"],
  "properties": {
    "requestId": {
      "type": "string",
      "minLength": 1,
      "description": "Caller-provided correlation identifier echoed by the shell response."
    },
    "subjectRef": {
      "type": "string",
      "minLength": 1,
      "description": "Opaque reference to the future quote, lock, or concession subject."
    },
    "evidenceRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "default": [],
      "description": "Opaque future evidence references; empty is valid for this shell slice."
    },
    "ruleSetRef": {
      "type": "string",
      "minLength": 1,
      "description": "Optional external or configured rule-set reference, not an embedded regulatory threshold."
    }
  }
}
